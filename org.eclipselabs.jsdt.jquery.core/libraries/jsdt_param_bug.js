/**
 * Accepts a string containing a CSS selector which is then used to match a set of elements.
 */
function jQuery() {};

/**
 * Adds the specified class(es) to each of the set of matched elements.
 * @name addClass^1
 * @param {String} className One or more class names to be added to the class attribute of each matched element.
 * @returns jQuery
 */
/**
 * @name addClass^2
 * @param {Function} function(index, currentClass) A function returning one or more space-separated class names to be added. Receives the index position of the element in the set and the old class value as arguments.
 */
jQuery.prototype.addClass = function() {};